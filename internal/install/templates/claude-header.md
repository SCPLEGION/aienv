---
name: aienv-secrets
description: "Use whenever a task involves getting an API key, token, password, or other secret into a file (.env, config.yaml, docker-compose.yml, application.properties, etc.) on a machine that has the `aienv` CLI available, or whenever the user asks to store, check, list, rotate, or remove a secret. Also check this skill before writing, catting, or grepping any file that might contain a secret-looking value (sk-..., AKIA..., ghp_..., password: ...) — aienv has dedicated commands for all of that so the value never has to pass through the agent's context."
---

# aienv-secrets
